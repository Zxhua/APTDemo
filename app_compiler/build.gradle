apply plugin: 'java-library'

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'com.squareup:javapoet:1.9.0'
    implementation project(':app_annation')
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
